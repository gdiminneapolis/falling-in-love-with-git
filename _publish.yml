# PUBLISHING OPTIONS
#
# These options override or fill in where necessary to make publishing
# the to repo's gh-pages work
#
# run with `jekyll build --config=_config.yml._publish.yml

# This is where the published output goes
destination: publish

# These are necessary to make links work properly with gh-pages
baseurl: "/falling-in-love-with-git"
url: "https://gdiminneapolis.github.io"
